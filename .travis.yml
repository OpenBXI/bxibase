language: c
sudo: required
compiler:
        - clang
        - gcc

before_install:
        - sudo apt-get update -qq
        - sudo apt-get install -qq libtool pkg-config libzmq5 build-essential autoconf automake

install:
        - ./misc/travis/install-dependencies.sh     
        - ./bootstrap.sh

